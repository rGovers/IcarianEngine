#version 450

layout(location = 0) in vec2 vUV;

#!pushtexture(0, tex)

#!pushbuffer(PUIBuffer, uiBuffer)

layout(location = 0) out vec4 outColor;

void main() 
{
    outColor = uiBuffer.Color * texture(tex, vUV);
}